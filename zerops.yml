zerops:
  - setup: mailpit
    build:
      buildCommands:
        - sh download-mailpit.sh
      deployFiles: ./mailpit
    run: 
      base: go@1
      ports:
        - port: 8025
          httpSupport: true
        - port: 1025
      start: ./mailpit
